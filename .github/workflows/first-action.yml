name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: printing greeting
        run: echo "Hello world"
      - name: printing goodby
        run: echo "Done - bye!"
